export type User = {
    id_user: number;
    username_user: string;
    password_user: string;
}

export type Admin = {
    id_admin: string;
    username_admin: string;
    password_admin: string;
}